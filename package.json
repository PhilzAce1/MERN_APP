{
  "name": "shopping_list",
  "version": "1.0.0",
  "description": "shopping list using the mern stack ",
  "main": "server.js",
  "scripts": {
    "client-install": "npm install --prefix client",
    "start": "node server.js",
    "server": "nodemon server.js",
    "cloned": "npm install && npm install --prefix client",
    "client": "npm start --prefix client",
    "dev": "concurrently \"npm run server\" \"npm run client\"",
    "heroku-postbuild": "NPM_CONFIG_PRODUCTION=false npm install --prefix client && npm run build --prefix client",
    "test": "echo \"Error: no test specified\" && exit 1",
    "quick-develop": "git add . && git commit -m 'quick save' && git push heroku master"
  },
  "keywords": [
    "MERN",
    "express",
    "React"
  ],
  "author": "Akuagwu Philemon",
  "license": "MIT",
  "dependencies": {
    "bcryptjs": "^2.4.3",
    "bootstrap": "^4.3.1",
    "concurrently": "^5.0.0",
    "dotenv": "^8.1.0",
    "express": "^4.17.1",
    "jsonwebtoken": "^8.5.1",
    "mongoose": "^5.7.5",
    "react-bootstrap": "^0.32.4"
  },
  "devDependencies": {
    "nodemon": "^1.19.4"
  }
}
